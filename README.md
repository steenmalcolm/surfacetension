# Surface Tension Package
## Installation

To install the package, run the following commands

```bash
pip install -r requirements.txt
pip install -e .
```

Note you need `phasesep` to be installed locally.